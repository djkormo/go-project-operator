---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: projects.project.djkormo.github.io
spec:
  group: project.djkormo.github.io
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project is the Schema for the projects API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ProjectSpec defines the desired state of Project
            properties:
              limitRange:
                description: ResourceQuota specification
                properties:
                  maxCpu:
                    default: 2000m
                    description: Maksimum for CPU
                    type: string
                  maxMem:
                    default: 20Gi
                    description: Maksimum for Memory
                    type: string
                  minCpu:
                    default: 50m
                    description: Minimum for CPU
                    type: string
                  minMem:
                    default: 50Mi
                    description: Minimum for Memory
                    type: string
                required:
                - maxCpu
                - maxMem
                - minCpu
                - minMem
                type: object
              resourceQuota:
                description: ResourceQuota specification
                properties:
                  limitsCpu:
                    default: "2"
                    description: Limits for CPU
                    type: string
                  limitsMemory:
                    default: 4G
                    description: Limits for Memory
                    type: string
                  requestsCpu:
                    default: "2.5"
                    description: Requests for CPU
                    type: string
                  requestsMemory:
                    default: 1G
                    description: Requests for Memory
                    type: string
                required:
                - limitsCpu
                - limitsMemory
                - requestsCpu
                - requestsMemory
                type: object
            required:
            - limitRange
            - resourceQuota
            type: object
          status:
            description: ProjectStatus defines the observed state of Project
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
